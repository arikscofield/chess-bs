{
  "name": "chess-bs-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "common",
    "client",
    "server"
  ],
  "scripts": {
    "dev:server": "npm run start --workspace=server",
    "dev:client": "npm run dev --workspace=client"
  }
}